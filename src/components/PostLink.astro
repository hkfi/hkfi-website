---
import type { PageObjectResponse } from '@notionhq/client/build/src/api-endpoints'

type Props = {
  post: PageObjectResponse
}

const { post } = Astro.props

const slug =
  'rich_text' in post.properties.Slug
    ? post.properties.Slug.rich_text[0].plain_text
    : ''

const title =
  'title' in post.properties.Title && post.properties.Title.title[0]
    ? post.properties.Title.title[0].plain_text
    : ''

const date =
  'date' in post.properties.Date && post.properties.Date.date
    ? post.properties.Date.date.start
    : ''
---

<a
  class='text-text ease-in duration-75 text-2xl origin-left hover:bg-secondary py-2 px-1 flex justify-between'
  href={`/blog/post/${slug}`}
>
  <span>
    {title}
  </span>
  <span>
    {date}
  </span>
</a>
