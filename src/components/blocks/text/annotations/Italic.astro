---
import type { RichTextItemResponse } from '@notionhq/client/build/src/api-endpoints'

export type Props = {
  richText: RichTextItemResponse
}

const { richText } = Astro.props
---

<!-- prettier-ignore -->
{richText.annotations.italic ? <i><slot /></i> : <slot />}
